databases:
  - name: tentsauna_app
    databaseName: tentsauna_app
    user: tentsauna
    region: singapore

services:
  - type: web
    name: tentsauna
    env: ruby
    region: singapore
    plan: free
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: tentsauna_app
          property: connectionString
      # - key: RAILS_MASTER_KEY # TODO: 設定必要 
        # sync: false # MEMO: Renderのダッシュボード側からも設定できるが、明示的に示したほうが良いコード
    # autoDeploy: false # MEMO: Renderのダッシュボード側からも設定できるが、明示的に示したほうが良いコード
